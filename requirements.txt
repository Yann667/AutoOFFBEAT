# AutoOFFBEAT : dépendances Python
# Installer avec : pip install -r requirements.txt

# ── GUI ───────────────────────────────────────────────────────────────────────
dash>=2.17
dash-bootstrap-components>=1.6

# ── LangChain 1.x + LangGraph (API create_agent) ─────────────────────────────
langchain>=1.0,<2.0
langchain-core>=1.0,<2.0
langgraph>=1.0,<2.0

# ── Fournisseurs LLM (installer selon LLM_PROVIDER) ──────────────────────────
langchain-anthropic>=1.0        # LLM_PROVIDER=anthropic
langchain-google-genai>=3.0     # LLM_PROVIDER=gemini
langchain-ollama>=1.0           # LLM_PROVIDER=ollama

# ── Post-traitement ───────────────────────────────────────────────────────────
pyvista>=0.44
numpy>=1.26

# ── RAG (assistant documentaire) ──────────────────────────────────────────────
faiss-cpu>=1.8          # magasin vectoriel local (léger, sans serveur)
pypdf>=4.0              # lecture des PDF du corpus
langchain-community>=0.3   # tools/rag_retriever.py : FAISS, TextLoader, PyPDFLoader
langchain-text-splitters>=1.0  # tools/rag_retriever.py : RecursiveCharacterTextSplitter
# embeddings : Ollama nomic-embed-text (ollama pull nomic-embed-text)

# ── Jumeau numérique D5 : surrogate (émulateur temps réel) ────────────────────
scikit-learn>=1.4       # régression de l'émulateur (surface de réponse)
joblib>=1.3             # persistance du modèle entraîné

# ── Utilitaires ───────────────────────────────────────────────────────────────
python-dotenv>=1.0
pydantic>=2.0
matplotlib>=3.8         # tools/data_processor.py : figures PNG, backend Agg
